# Binaries
/bin/
/dist/
/out/
*.exe
*.test
*.out

# Go
coverage.out
coverage.html
vendor/

# Local env
.env
.env.*
!.env.example

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Compose / data
*.log
/data/

# Working notes and agent config — keep locally, do not publish
/AGENTS.md
/PROJECT_STATE.md
/.cursor/
/docs/*
!/docs/SECURITY.md

# Native supervisor build products (sources in executor/ are tracked)
executor/aquila-exec
executor/aquila-exec-test

# Terraform local state and secrets (sources in deploy/terraform/ are tracked)
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/terraform.tfvars
deploy/terraform/crash.log

# Empty phase placeholders
**/.gitkeep
/benchmarks/
/chaos/
/proto/
/web/
/deploy/kubernetes/
/internal/traces/
/internal/artifacts/
/internal/cache/
/internal/auth/
/internal/dag/
/internal/scheduler/
/internal/agent/
/internal/lease/
